# Claim RTN-302 — Flujo de reciclaje con cámara

## Lease

- **Estado:** `RELEASED`
- **Owner:** Codex
- **Colaboradores:** ninguno
- **Rama:** `feat/RTN-302-recycling-flow`
- **Inicio UTC:** `2026-08-14T19:10:22Z`
- **Último heartbeat UTC:** `2026-08-14T19:44:52Z`
- **Vence UTC:** `2026-08-15T03:10:22Z`
- **PR:** `https://github.com/caiuc/equipo-14-haCAIthon-2026/pull/4`

## Objetivo

Implementar el flujo principal de registro de reciclaje con selección manual, escaneo de códigos de barra con cámara, búsqueda de productos y confirmación con comunidad receptora obligatoria.

## Plan

1. Construir la pantalla de reciclaje con modos escáner/manual, comunidad, cantidad, preview de impacto y confirmación contra el store demo.
2. Agregar componentes de reciclaje para cámara, tarjeta de producto, selector de categoría y preview de impacto.
3. Crear el proveedor de búsqueda de productos con catálogo local, normalización de barcodes, adaptador Open Food Facts y fallback manual.
4. Cubrir reglas críticas de lookup/preview con tests unitarios y verificar typecheck, lint y test.
5. Actualizar tareas, progreso, evidencia y liberar el claim al cerrar.

## Write set exclusivo

- `app/recycle.tsx`
- `src/features/recycling/**`
- `src/services/barcode/**`

## Shared write set

- `.env.example` — documentar bypass local temporal.
- `app/_layout.tsx` — permitir `/recycle` sin sesión sólo en modo preview local.
- `app/index.tsx` — redirigir a `/recycle` cuando el preview local esté activo.
- `src/auth/preview.ts` — centralizar bandera local de preview.
- `docs/TASKS.md` — actualizar estado/owner y evidencia.
- `docs/PROGRESS.md` — trabajo activo, heartbeat y handoff obligatorio.

## Dependencias y contratos

- Cubre RTN-302, RTN-701, RTN-702 y RTN-703 en una rama integrada.
- Usa las comunidades ya unidas del store demo mientras RTN-204 siga pendiente.
- No implementa backend ni RPC; mantiene compatibilidad con el futuro ledger transaccional.
- `expo-camera` ya está instalado y configurado en `app.json`.

## Verificación prevista

- `npm run typecheck`
- `npm run lint`
- `npm test`
- Revisión manual en web de permisos concedidos/denegados, producto encontrado/no encontrado y registro manual.

## Handoff / liberación

- Implementación disponible localmente en la rama `feat/RTN-302-recycling-flow`.
- Se agregaron pantalla de reciclaje, componentes de cámara/manual, contrato de búsqueda, adaptador Open Food Facts, catálogo local y tests de lookup.
- Se agregó bypass local de preview para `/recycle` cuando no hay Supabase configurado o `EXPO_PUBLIC_SKIP_AUTH_FOR_RECYCLE=true`.
- Verificación ejecutada: `npm run typecheck`, `npm run lint`, `npm test`, `EXPO_PUBLIC_SKIP_AUTH_FOR_RECYCLE=true npm run web:export`.
- Prueba local: `python3 -m http.server 8090 --directory dist` responde `200 OK` en `http://localhost:8090/recycle.html`.
- Queda pendiente prueba manual con cámara física en Android y recorrido de permisos en navegador/dispositivo.
- Write set liberado el `2026-08-14T19:44:52Z`.
