# Claim RTN-806 — Supabase remoto + deploy Vercel

## Lease

- **Estado:** `RELEASED`
- **Owner:** Claude (sesión dirigida por Benjamín Taito)
- **Colaboradores:** ninguno
- **Rama:** `chore/RTN-806-supabase-vercel-deploy`
- **Inicio UTC:** `2026-08-14T18:30:00Z`
- **Último heartbeat UTC:** `2026-08-14T19:10:00Z`
- **Vence UTC:** `n/a (cerrado en la misma sesión)`
- **PR:** pendiente al publicar el plan commit

## Objetivo

Crear el proyecto Supabase remoto de Retorna, aplicar el esquema existente, dejar el cliente Expo web deployable en Vercel y publicar un deploy de producción real.

## Plan

1. Crear proyecto Supabase remoto y aplicar `supabase/migrations/20260814190000_users_and_organizations.sql`.
2. Verificar advisories de seguridad y que el trigger `handle_new_user` funciona (prueba SQL directa, sin depender de confirmación de correo).
3. Corregir el bug de export estático que rompía el build (`window is not defined` durante SSG) y el ruteo 404 en Vercel.
4. Compilar `web:export` en un worktree aislado de `develop` (para no interferir con ramas de otros agentes activas en el mismo checkout) y desplegar a producción con Vercel CLI.
5. Documentar el bloqueo pendiente (confirmación de correo por defecto en el proyecto Supabase Cloud) que no se puede resolver con las herramientas MCP disponibles.

## Write set exclusivo

- `src/data/supabase.ts` (fix SSR-safe storage)
- `public/vercel.json` (rewrites + cleanUrls para el export estático)
- `docs/claims/RTN-806.md`

## Shared write set

- `docs/TASKS.md` — nueva fila RTN-806.
- `docs/PROGRESS.md` — heartbeat y handoff.

## Dependencias y contratos

- RTN-101–106 (esquema y RLS de usuarios/organizaciones), ya escritos por trabajo previo.
- No se tocó ningún archivo bajo trabajo activo de otros agentes (`RTN-302`, `RTN-307`); el build/deploy se hizo en un worktree separado sobre `origin/develop`.

## Verificación prevista

- `npm run typecheck`, `npm run lint`, `npm test`, `npm run web:export` — todos OK en el worktree aislado.
- Prueba SQL directa contra Postgres remoto: inserción en `auth.users` genera fila en `public.profiles` vía trigger (luego eliminada).
- Deploy de producción verificado en navegador: `/`, `/sign-up` y `/sign-in` renderizan sin errores de consola.
- `get_advisors` (security): sólo warnings esperables de funciones `SECURITY DEFINER` expuestas intencionalmente vía RPC (no bloqueante).

## Handoff / liberación

- **Qué cambió:** proyecto Supabase remoto `retorna` (`bhjplveltcqocbhfmbhn`, sa-east-1) con el esquema de usuarios/organizaciones aplicado; fix de SSR en el cliente Supabase; `public/vercel.json` con `cleanUrls` y rewrites para rutas dinámicas; deploy de producción en `https://dist-five-pearl-95.vercel.app` (proyecto Vercel `retorna`, cuenta personal `benjamintaito-7391`).
- **Qué está verificado:** build/typecheck/lint/test en `develop`; esquema y trigger de perfiles funcionando en la base remota; deploy en producción sirviendo todas las rutas sin 404.
- **Qué no se pudo verificar:** el flujo de registro real desde la UI, porque el proyecto Supabase Cloud exige confirmación de correo por defecto (a diferencia de `supabase/config.toml`, que sólo aplica a `supabase start` local) y no hay herramienta MCP para cambiar esa configuración de Auth. Se requiere desactivar manualmente "Confirm email" en el dashboard: `https://supabase.com/dashboard/project/bhjplveltcqocbhfmbhn/auth/providers`.
- **Qué contratos o variables cambiaron:** ninguno en el esquema; se agregó `public/vercel.json` (nuevo) y se ajustó el storage adapter web de `src/data/supabase.ts` (mismo contrato público, sin cambios de API).
- **Siguiente tarea desbloqueada:** desactivar confirmación de correo en el dashboard de Supabase y repetir el flujo registro → organizaciones → solicitud/aprobación end-to-end contra producción; luego apuntar el dominio `retorna.lat` (en compra) al proyecto Vercel.
